import { ToggleAddressDirective } from './toggle-address-directive';

describe('ToggleAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleAddressDirective();
    expect(directive).toBeTruthy();
  });
});
